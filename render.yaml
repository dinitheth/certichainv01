services:
  - type: web
    name: certichain-api
    runtime: node
    env: node
    buildCommand: npm install
    startCommand: npm run server
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NODE_VERSION
        value: 18
